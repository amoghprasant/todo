name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: "^3.5.1"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  flutter_local_notifications: ^13.0.0
  timezone: ^0.9.0  # Updated to match the dependency requirement
  path_provider: ^2.0.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
